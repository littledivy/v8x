diff --git a/core/iwasm/common/wasm_c_api.c b/core/iwasm/common/wasm_c_api.c
index 9fba0a0..9fba0a0 100644
--- a/core/iwasm/common/wasm_c_api.c
+++ b/core/iwasm/common/wasm_c_api.c
@@ -1664,6 +1664,15 @@ rt_val_to_wasm_val(const uint8 *data, uint8 val_type_rt, wasm_val_t *out)
                                              (void **)&out->of.ref);
             }
             break;
+        case VALUE_TYPE_FUNCREF:
+            out->kind = WASM_FUNCREF;
+            if (NULL_REF == *(uint32 *)data) {
+                out->of.ref = NULL;
+            }
+            else {
+                ret = false;
+            }
+            break;
 #endif
         default:
             LOG_WARNING("unexpected value type %d", val_type_rt);

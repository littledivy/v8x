diff --git a/quickjs-opcode.h b/quickjs-opcode.h
index 3bb7b4d..6d7fe8b 100644
--- a/quickjs-opcode.h
+++ b/quickjs-opcode.h
@@ -269,6 +269,7 @@ DEF(using_dispose_async, 3, 0, 1, loc)
 DEF(using_dispose_merge, 1, 2, 1, none)
 DEF(  using_dispose_end, 1, 1, 0, none)
 DEF(        using_check, 2, 1, 2, u8)
+DEF(       debugger, 1, 0, 0, none)
 /* must be the last non short and non temporary opcode */
 DEF(            nop, 1, 0, 0, none)
 
diff --git a/quickjs.c b/quickjs.c
index c777e54..c17e92d 100644
--- a/quickjs.c
+++ b/quickjs.c
@@ -8386,6 +8386,7 @@ extern void v82jsc_coverage_range_hit(const void *key,
                                       uint32_t start,
                                       uint32_t end);
 extern void v82jsc_coverage_function_hit(const void *key);
+extern void v82jsc_debugger_statement(void);
 
 void JS_SetPreciseCoverageEnabled(bool enabled)
 {
@@ -21471,6 +21472,9 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
             ret_val = JS_UNDEFINED;
             goto done_generator;
 
+        CASE(OP_debugger):
+            v82jsc_debugger_statement();
+            BREAK;
         CASE(OP_nop):
             BREAK;
         CASE(OP_is_undefined_or_null):
@@ -31762,11 +31766,12 @@ static __exception int js_parse_statement_or_decl(JSParseState *s,
         break;
 
     case TOK_DEBUGGER:
-        /* currently no debugger, so just skip the keyword */
+        emit_source_loc(s);
         if (next_token(s))
             goto fail;
         if (js_parse_expect_semi(s))
             goto fail;
+        emit_op(s, OP_debugger);
         break;
 
     case TOK_ENUM:

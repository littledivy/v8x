diff --git a/quickjs.c b/quickjs.c
index 241c217..e04f6bb 100644
--- a/quickjs.c
+++ b/quickjs.c
@@ -18400,7 +18400,10 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
         [ OP_COUNT ... 255 ] = &&case_default
     };
     __extension__ static const void * const coverage_dispatch_table[256] = {
-        [ 0 ... 255 ] = &&coverage_dispatch
+#define DEF(id, size, n_pop, n_push, f) && coverage_OP_ ## id,
+#define def(id, size, n_pop, n_push, f)
+#include "quickjs-opcode.h"
+        [ OP_COUNT ... 255 ] = &&case_default
     };
     active_dispatch_table = rt->v82jsc_precise_coverage_enabled
                                 ? coverage_dispatch_table
@@ -18410,9 +18413,9 @@ static JSValue JS_CallInternal(JSContext *caller_ctx, JSValueConst func_obj,
 #define DEFAULT         case_default
 #define BREAK           SWITCH(pc)
     goto coverage_dispatch_done;
-coverage_dispatch:
-    js_coverage_record_hit(b, (uint32_t)(pc - b->byte_code_buf - 1));
-    __extension__ ({ goto *dispatch_table[opcode]; });
+#define DEF(id, size, n_pop, n_push, f) coverage_OP_ ## id: js_coverage_record_hit(b, (uint32_t)(pc - b->byte_code_buf - 1)); goto case_OP_ ## id;
+#define def(id, size, n_pop, n_push, f)
+#include "quickjs-opcode.h"
 coverage_dispatch_done:
 #endif
 

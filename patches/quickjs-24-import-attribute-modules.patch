--- a/quickjs.c
+++ b/quickjs.c
@@ -30153,14 +30153,6 @@
                                 JSAtom module_name)
 {
     JSReqModuleEntry *rme;
-    int i;
-
-    /* no need to add the module request if it is already present */
-    for(i = 0; i < m->req_module_entries_count; i++) {
-        rme = &m->req_module_entries[i];
-        if (rme->module_name == module_name)
-            return i;
-    }
 
     if (js_resize_array(ctx, (void **)&m->req_module_entries,
                         sizeof(JSReqModuleEntry),
@@ -30171,7 +30163,7 @@
     rme->module_name = JS_DupAtom(ctx, module_name);
     rme->module = NULL;
     rme->attributes = JS_UNDEFINED;
-    return i;
+    return m->req_module_entries_count - 1;
 }
 
 #endif // QJS_DISABLE_PARSER
@@ -30450,12 +30442,15 @@
         return NULL;
     }
 
-    /* first look at the loaded modules */
-    m = js_find_loaded_module(ctx, module_name);
-    if (m) {
-        js_free(ctx, cname);
-        JS_FreeAtom(ctx, module_name);
-        return m;
+    /* An attributes-aware host owns module identity because the same normalized
+       name may resolve to different modules for different attributes. */
+    if (!rt->module_loader_has_attr) {
+        m = js_find_loaded_module(ctx, module_name);
+        if (m) {
+            js_free(ctx, cname);
+            JS_FreeAtom(ctx, module_name);
+            return m;
+        }
     }
 
     JS_FreeAtom(ctx, module_name);
@@ -38153,7 +38148,7 @@
     BC_TAG_SNAPSHOT_INTRINSIC,
 } BCTagEnum;
 
-#define BC_VERSION 26
+#define BC_VERSION 27
 
 typedef struct BCWriterState {
     JSContext *ctx;
@@ -39205,6 +39200,8 @@
     for(i = 0; i < m->req_module_entries_count; i++) {
         JSReqModuleEntry *rme = &m->req_module_entries[i];
         bc_put_atom(s, rme->module_name);
+        if (JS_WriteObjectRec(s, rme->attributes))
+            goto fail;
     }
 
     bc_put_leb128(s, m->export_entries_count);
@@ -41087,6 +41084,9 @@
             JSModuleDef **pm = &rme->module;
             if (bc_get_atom(s, &rme->module_name))
                 goto fail;
+            rme->attributes = JS_ReadObjectRec(s);
+            if (JS_IsException(rme->attributes))
+                goto fail;
             // Resolves a module either from the cache or by requesting
             // it from the module loader. From cache is not ideal because
             // the module may not be the one it was a time of serialization
diff --git a/builtin-array-fromasync.h b/builtin-array-fromasync.h
--- a/builtin-array-fromasync.h
+++ b/builtin-array-fromasync.h
@@ -6,4 +6,4 @@ const uint32_t qjsc_builtin_array_fromasync_size = 888;
 
 const uint8_t qjsc_builtin_array_fromasync[888] = {
- 0x1a, 0xc5, 0x39, 0x53, 0x3c, 0x0e, 0x01, 0x28,
+ 0x1b, 0xc5, 0x39, 0x53, 0x3c, 0x0e, 0x01, 0x28,
  0x53, 0x79, 0x6d, 0x62, 0x6f, 0x6c, 0xb7, 0x61,
diff --git a/builtin-iterator-zip.h b/builtin-iterator-zip.h
--- a/builtin-iterator-zip.h
+++ b/builtin-iterator-zip.h
@@ -6,4 +6,4 @@ const uint32_t qjsc_builtin_iterator_zip_size = 2621;
 
 const uint8_t qjsc_builtin_iterator_zip[2621] = {
- 0x1a, 0xca, 0x2d, 0xe4, 0xa6, 0x2a, 0x01, 0x1c,
+ 0x1b, 0xca, 0x2d, 0xe4, 0xa6, 0x2a, 0x01, 0x1c,
  0x49, 0x74, 0x65, 0x72, 0x61, 0x74, 0x6f, 0x72,
diff --git a/builtin-iterator-zip-keyed.h b/builtin-iterator-zip-keyed.h
--- a/builtin-iterator-zip-keyed.h
+++ b/builtin-iterator-zip-keyed.h
@@ -6,4 +6,4 @@ const uint32_t qjsc_builtin_iterator_zip_keyed_size = 2582;
 
 const uint8_t qjsc_builtin_iterator_zip_keyed[2582] = {
- 0x1a, 0xeb, 0x00, 0x6f, 0x50, 0x2b, 0x01, 0x1c,
+ 0x1b, 0xeb, 0x00, 0x6f, 0x50, 0x2b, 0x01, 0x1c,
  0x49, 0x74, 0x65, 0x72, 0x61, 0x74, 0x6f, 0x72,
